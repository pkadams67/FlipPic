import UIKit

enum FiltersController {
    static func updateWithFilter(filter: Filter, flipPicImage: FlipPicImage) {
        print("TEST: Filter '\(filter)' applied to image")
        // create a new image from original
        // apply filter to new image
        // set flipPicImage to new image
    }
}
